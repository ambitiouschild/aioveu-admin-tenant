看了你的代码，我找到了几个关键问题。让我逐一分析并修复：

## 问题分析

### 1. **第一个 SKU 编码列显示异常**

你的 SKU 编码列使用了 `el-form-item`嵌套在 `el-table-column`中，这种方式在 Element Plus 中容易导致表单验证绑定错误。特别是第一个 SKU 的输入框变成了"数量框"。

### 2. **`specIds`格式不一致**

在 `generateSkuList`中，你生成了 `specIds`但是查找现有 SKU 时可能找不到匹配，因为编辑模式下后端的 `specIds`格式可能不同。

### 3. **SKU 生成逻辑中现有 SKU 查找失败**

你的查找逻辑有问题：

```
const existingSku = skuForm.value.skuList.find((sku) => {
  return sku.specIds === specIds || sku.specValues === specValues;
});
```

如果 `skuForm.value.skuList`是空的（新增模式）或者规格值格式不匹配，就会找不到现有 SKU。

## 修复方案

### 1. 修复 SKU 编码列的显示问题

移除不必要的 `el-form-item`包裹，改为直接在表格列中绑定：

```
<!-- SKU编码列 -->
<el-table-column label="SKU编码" width="180" align="center">
  <template #default="scope">
    <!-- 移除 el-form-item 包裹，直接使用 el-input -->
    <el-input 
      v-model="scope.row.skuSn" 
      placeholder="如: SKU001" 
      clearable
      size="small"
      @blur="validateSkuSn(scope.$index)"
    />
  </template>
</el-table-column>
```

然后在 JS 中添加单独的验证方法：

```
// SKU编码验证方法
const validateSkuSn = (index: number) => {
  const sku = skuForm.value.skuList[index];
  if (!sku.skuSn?.trim()) {
    ElMessage.warning(`第 ${index + 1} 个SKU编码不能为空`);
    return false;
  }
  if (sku.skuSn.length > 50) {
    ElMessage.warning(`第 ${index + 1} 个SKU编码长度不能超过50字符`);
    return false;
  }
  return true;
};
```

### 2. 修复现有 SKU 查找逻辑

在 `generateSkuList`中，改进查找逻辑：

```
// 查找现有SKU - 修复版
const findExistingSku = (specIds: string, specValues: string): SkuItem | undefined => {
  // 方法1：先尝试通过 specIds 查找
  let existingSku = skuForm.value.skuList.find(sku => sku.specIds === specIds);
  
  // 方法2：如果找不到，尝试通过 specValues 查找
  if (!existingSku) {
    existingSku = skuForm.value.skuList.find(sku => sku.specValues === specValues);
  }
  
  // 方法3：如果还找不到，尝试通过规格值组合的模糊匹配
  if (!existingSku && specValues) {
    existingSku = skuForm.value.skuList.find(sku => {
      if (!sku.specValues) return false;
      // 分割规格值进行比较
      const skuValues = sku.specValues.split('_');
      const newValues = specValues.split('_');
      return skuValues.length === newValues.length && 
             skuValues.every((val, idx) => val === newValues[idx]);
    });
  }
  
  return existingSku;
};
```

然后在生成 SKU 时使用：

```
// 生成SKU列表
const newSkus: SkuItem[] = valueCombinations.map((values: SpecValue[], index) => {
  const specValues = values.map((v) => v.value).join("_");
  const specIds = values.map((v) => v.id).join("_");

  // 查找现有SKU
  const existingSku = findExistingSku(specIds, specValues);

  console.log(`🔍 查找现有SKU结果:`, existingSku ? '找到' : '未找到');

  // 构建SKU对象
  const sku: SkuItem = {
    skuSn: existingSku?.skuSn || 
      `SKU_${Date.now().toString(36).substr(2, 6)}_${String(index + 1).padStart(3, '0')}`.toUpperCase(),
    price: existingSku?.price || 0,
    stock: existingSku?.stock || 0,
    Lockedstock: existingSku?.Lockedstock || 0,
    specIds,
    specValues,
    id: existingSku?.id,
    picUrl: existingSku?.picUrl || "",
  };

  // 添加动态规格字段
  values.forEach((value, i) => {
    sku[`specValue${i + 1}`] = value.value;
  });

  return sku;
});
```

### 3. 修复表单验证

简化表单验证，移除表格内的复杂表单结构：

```
<!-- 库存设置卡片 -->
<el-card
  class="box-card"
  shadow="never"
  v-if="specForm.specList.some((spec) => spec.values && spec.values.length > 0)"
  style="margin-top: 20px"
>
  <template #header>
    <div class="card-header">
      <span class="card-title">商品库存</span>
      <span class="sku-count">共 {{ skuForm.skuList.length }} 个SKU</span>
    </div>
  </template>

  <!-- 直接使用 el-table，不在内部嵌套 el-form -->
  <el-table
    :data="skuForm.skuList"
    border
    stripe
    size="small"
    class="sku-table"
    v-if="skuForm.skuList.length > 0"
  >
    <!-- 动态生成规格列 -->
    <el-table-column
      v-for="(title, index) in specTitles"
      :key="index"
      :label="title"
      align="center"
      width="120"
    >
      <template #default="scope">
        <span class="spec-value">{{ scope.row[`specValue${index + 1}`] }}</span>
      </template>
    </el-table-column>

    <!-- SKU编码列 -->
    <el-table-column label="SKU编码" width="180" align="center">
      <template #default="scope">
        <el-input 
          v-model="scope.row.skuSn" 
          placeholder="如: SKU001" 
          clearable
          size="small"
          @blur="validateSkuSn(scope.$index)"
        />
      </template>
    </el-table-column>

    <!-- 价格列 -->
    <el-table-column label="价格(元)" width="150" align="center">
      <template #default="scope">
        <el-input
          v-model.number="scope.row.price"
          type="number"
          placeholder="0.00"
          :min="0"
          :step="0.01"
          size="small"
          @input="handlePriceChange(scope.row)"
        >
          <template #prefix>¥</template>
        </el-input>
      </template>
    </el-table-column>

    <!-- 库存列 -->
    <el-table-column label="库存" width="120" align="center">
      <template #default="scope">
        <el-input
          v-model.number="scope.row.stock"
          type="number"
          placeholder="0"
          :min="0"
          :step="1"
          size="small"
        />
      </template>
    </el-table-column>

    <!-- SKU图片列保持不变 -->
    <!-- ... -->
  </el-table>

  <!-- SKU空状态 -->
  <div v-else class="empty-state">
    <el-empty description="请先配置规格值" :image-size="100" />
  </div>
</el-card>
```

### 4. 修改提交时的验证逻辑

在 `handleSubmit`方法中，修改验证逻辑：

```
/**
 * 提交表单
 */
const handleSubmit = async (): Promise<void> => {
  try {
    console.log("📤 开始提交表单");

    // 1. 检查必填字段
    if (!goodsInfo.value.categoryId) {
      ElMessage.warning("请先选择商品分类");
      return;
    }

    // 2. 检查规格值
    const hasSpecValues = specForm.value.specList.some(
      (spec) => spec.values && spec.values.length > 0
    );

    if (!hasSpecValues) {
      ElMessage.warning("请至少为一个规格添加规格值");
      return;
    }

    // 3. 检查是否有SKU
    if (skuForm.value.skuList.length === 0) {
      ElMessage.warning("未生成商品SKU");
      return;
    }

    // 4. 验证所有SKU的必填字段
    const invalidSkus = skuForm.value.skuList.filter((sku, index) => {
      if (!sku.skuSn?.trim()) {
        ElMessage.warning(`第 ${index + 1} 个SKU编码不能为空`);
        return true;
      }
      if (sku.price === undefined || sku.price === null || sku.price < 0) {
        ElMessage.warning(`第 ${index + 1} 个SKU价格无效`);
        return true;
      }
      if (sku.stock === undefined || sku.stock === null || sku.stock < 0) {
        ElMessage.warning(`第 ${index + 1} 个SKU库存无效`);
        return true;
      }
      return false;
    });

    if (invalidSkus.length > 0) {
      return;
    }

    // 5. 准备提交数据...
    // ... 后续代码保持不变
  } catch (error: any) {
    console.error("❌ 提交失败:", error);
    // ... 错误处理
  }
};
```

## 总结

主要修改：

1. **移除了表格内的复杂表单结构**，简化了验证逻辑
2. **改进了现有 SKU 查找算法**，提高匹配成功率
3. **添加了独立的验证方法**，避免 Element Plus 表单嵌套问题
4. **简化了提交验证逻辑**，直接验证数据而不是依赖表单组件

这样应该能解决你的问题。如果还有问题，请提供控制台的具体错误信息，我可以进一步帮你分析。