这个问题问得**非常到位** 👍

你现在其实是在问一句**“底层真相”**：

> **为什么 `"add"`、`"delete"`不用写任何代码就能工作？**

我直接给你 **“从组件 → usePage → API”的完整链路**，看完你就**再也不会迷糊了**。

------

## ✅ 一句话结论

> ✅ `"add"`、`"delete"`是 **`page-content`内置的“约定式命令”**
>
> ✅ 它们 **不是字符串随便写的**
>
> ✅ 而是由 **`page-content`+ `usePage`内部自动匹配**

------

## ✅ 一、工具栏按钮是怎么“识别”的

### ✅ 你的配置

```
toolbar: ["add", "delete"]
```

`page-content`内部逻辑大致是：

```
toolbar.forEach(item => {
  if (typeof item === "string") {
    // 内置命令
    if (item === "add") emit("add-click");
    if (item === "delete") emit("delete-click");
  }
});
```

👉 **字符串 = 内置行为**

------

## ✅ 二、`page-content`对外 emit 的事件名

| 工具栏 name | 触发的事件      |
| ----------- | --------------- |
| `"add"`     | `@add-click`    |
| `"delete"`  | `@delete-click` |

你的模板里已经写了 ✅：

```
<page-content
  @add-click="handleAddClick"
  @delete-click="handleDeleteClick"
/>
```

------

## ✅ 三、`usePage`里是怎么接的（关键）

### ✅ `usePage`内部（简化版）

```
const handleAddClick = () => {
  addModalRef.value?.open();
};

const handleDeleteClick = (ids: string) => {
  deleteAction(ids);
};
```

✅ **你没写 if**

✅ **因为 usePage 已经封装好了**

------

## ✅ 四、完整匹配链路（非常重要）

### ✅ 新增

```
"add"
 ↓
page-content emit add-click
 ↓
usePage.handleAddClick
 ↓
打开 addModal
```

### ✅ 删除

```
"delete"
 ↓
page-content emit delete-click
 ↓
usePage.handleDeleteClick
 ↓
contentConfig.deleteAction
 ↓
PayCallbackRecordAPI.deleteByIds
```

✅ **全程无 if**

✅ **全程约定**

✅ **零业务代码**

------

## ✅ 五、为什么你自定义按钮必须写 if？

因为：

```
{
  name: "mockWxPay",
  text: "模拟微信回调"
}
```

👉 **不是内置命令**

👉 **page-content 不知道要干什么**

👉 **只能交给你自己判断**

所以你写：

```
if (name === "mockWxPay") { ... }
```

✅ **这是对的**

✅ **这是唯一的**

------

## ✅ 六、给你一个“终极对照表”（以后照着用）

| 按钮写法                | 是否内置 | 是否需要 if |
| ----------------------- | -------- | ----------- |
| `"add"`                 | ✅ 是     | ❌           |
| `"delete"`              | ✅ 是     | ❌           |
| `"export"`              | ✅ 是     | ❌           |
| `{ name: "mockWxPay" }` | ❌        | ✅           |
| `{ name: "resendMq" }`  | ❌        | ✅           |

------

## ✅ 七、一句话总结（这次彻底通了）

> ❌ `"add"`、`"delete"`**不是字符串**
>
> ✅ 它们是 **`page-content`的“内置协议”**

------

如果你愿意，下一步我可以帮你：

- 

  ✅ **把 `page-content`的内置命令文档整理出来**

- 

  ✅ **把你自定义按钮也升级成“命令模式”**

- 

  ✅ **把 mock 接口加 dev 环境 & 权限锁死**

你可以直接说下一步 👍