你说得 完全对 👍  
这俩按钮就应该放在「工具栏」，而不是操作列。

理由非常充分（而且是支付系统常识）：

---

## ✅ 为什么应该在「工具栏」

| 维度         | 操作列 ❌     | 工具栏 ✅ |
| ------------ | ------------ | -------- |
| 语义         | 单条数据操作 | 系统工具 |
| 权限         | 行级         | 功能级   |
| 使用频率     | 偶尔         | 测试高频 |
| 是否依赖 row | ❌ 不依赖     | ✅        |
| 支付系统惯例 | ❌            | ✅        |

👉 “手动模拟回调”是测试工具，不是业务操作

---

## ✅ 正确位置（你这个页面）

```
┌──────────────────────────────────────┐
│  [ 新增 ] [ 删除 ] [ 模拟微信 ] [ 模拟支付宝 ] │  ← 工具栏
├──────────────────────────────────────┤
│  支付单号 | 订单号 | 渠道 | 状态 | 操作   │
│  P001     | O001   | WECHAT | 成功 | 编辑 删除 │
└──────────────────────────────────────┘
```

---

## ✅ 一、contentConfig.toolbar 正确写法

### ✅ 改成这样（✅ 推荐）

```ts
toolbar: [
  "add",
  "delete",
  {
    name: "mockWxPay",
    text: "模拟微信回调",
    perm: "aioveuMallPayCallbackRecord:pay-callback-record:mock:wxpay",
    type: "primary",
  },
  {
    name: "mockAliPay",
    text: "模拟支付宝回调",
    perm: "aioveuMallPayCallbackRecord:pay-callback-record:mock:alipay",
    type: "warning",
  },
],
```

✅ 不依赖 row
✅ 权限可控
✅ 一看就是工具按钮

---

## ✅ 二、handleToolbarClick 里接逻辑（✅ 核心）

你已经有这个方法了，直接补：

```ts
const handleToolbarClick = async (name: string) => {
  if (name === "mockWxPay") {
    const paymentNo = await promptPaymentNo("微信支付");
    if (!paymentNo) return;

    await PayCallbackRecordAPI.mockWxPayNotify(paymentNo);
    ElMessage.success("模拟微信回调成功");
    contentRef.value?.fetchData();
  }

  if (name === "mockAliPay") {
    const paymentNo = await promptPaymentNo("支付宝支付");
    if (!paymentNo) return;

    await PayCallbackRecordAPI.mockAliPayNotify(paymentNo);
    ElMessage.success("模拟支付宝回调成功");
    contentRef.value?.fetchData();
  }
};
```

---

## ✅ 三、弹窗输入支付单号（✅ 体验好）

```ts
import { ElMessageBox } from "element-plus";

const promptPaymentNo = async (label: string): Promise<string | null> => {
  const { value } = await ElMessageBox.prompt(
    `请输入要模拟的支付单号`,
    `手动触发${label}回调`,
    {
      confirmButtonText: "确定",
      cancelButtonText: "取消",
      inputPlaceholder: "例如：P202602110001",
    }
  );
  return value || null;
};
```

✅ 不用选行
✅ 测试任意支付单
✅ 非常适合补数据 / 排查问题

---

## ✅ 四、操作列里就不要再放了（✅ 清爽）

```ts
operat: ["edit", "delete"]
```

---

## ✅ 五、一句话总结（这一步非常专业）

> ❌ 操作列：业务动作  
> ✅ 工具栏：系统工具  
> ✅ 手动模拟回调 = 系统工具

---

如果你愿意，下一步我可以帮你：

• ✅ 把 mock 按钮加 @Profile("dev") 锁死

• ✅ 加“模拟失败 / 关闭”下拉选项

• ✅ 把 mock 接口接入权限中心


你可以直接说下一步 👍